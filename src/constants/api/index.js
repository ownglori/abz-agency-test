const URL = "https://frontend-test-assignment-api.abz.agency/api/v1";
const USERS = `${URL}/users`;
const POSITIONS = `${URL}/positions`;
const TOKEN = `${URL}/token`;

export const API = {
  URL: URL,
  USERS: USERS,
  POSITIONS: POSITIONS,
  TOKEN: TOKEN
};
